workflows:
  build:
    name: Build
    max_build_duration: 120
    environment:
      java: openjdk@17
      flutter: latest
    scripts:
      - name: Install dependencies
        script: flutter pub get
      - name: Build APK
        script: flutter build apk

